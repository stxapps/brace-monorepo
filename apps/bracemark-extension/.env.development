# Loaded by `wxt` / `wxt dev` (mode=development) — local development.
# Points at the local bracemark-api dev server (apps/bracemark-api, default PORT 8787).
# WXT_PUBLIC_* vars are baked into the bundle, so nothing secret belongs here.
WXT_PUBLIC_API_URL=http://localhost:8787
