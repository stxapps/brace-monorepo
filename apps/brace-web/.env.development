# Loaded automatically by `next dev` (NODE_ENV=development) — local development.
# Points at the local brace-api dev server (apps/brace-api, default PORT 8787).
NEXT_PUBLIC_API_URL=http://localhost:8787
