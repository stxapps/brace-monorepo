# Add files here to ignore them from prettier formatting
/dist
/coverage
/.nx/cache
/.nx/workspace-data
.nx/self-healing

# Build outputs & generated files
**/dist
**/out-tsc
**/.next
**/.output
**/.wxt
**/next-env.d.ts
# Regenerated by Uniwind on every metro run (including the one gradle's
# export:embed triggers during a release build), in its own style. Committed
# because tsconfig references it, so a clean clone's typecheck needs it — but
# formatting it here only makes it flip back on the next native build.
**/uniwind-env.d.ts

# Lockfiles
package-lock.json